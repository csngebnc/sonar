namespace SharedKernel.Enums
{
    public enum ParcelState
    {
        NEW = 0,
        AT_COURIER = 1,
        IN_WAREHOUSE = 2,
        IN_LOCKER = 3,
        DELIVERED = 4,
        DELIVERY_FAILED = 5,
        RETURN_STARTED = 6,
        RETURN_HAPPENED = 7
    }
}
